/*
Time & Space

Time: O(m * n)

Space: O(m * n)

Intuition (simple)

From any cell, you can only come from top or from left.
So ways to reach (i, j) = ways from top + ways from left.

DP meaning

dp[i][j] = number of unique paths to reach cell (i, j).

Base case

dp[0][0] = 1 → only one way to start.

First row → can only come from left.

First column → can only come from top.
*/
class Solution {
    public int uniquePaths(int m, int n) {
        int[][] dp = new int[m][n];
        dp[0][0]=1;
        for(int i=0;i<m;i++){
            for(int j=0;j<n;j++){
                if(j-1>=0){
                    dp[i][j] = dp[i][j-1];
                }
                if(i-1>=0){
                    dp[i][j]+=dp[i-1][j];
                }
            }
        }
        return dp[m-1][n-1];
    }
}
