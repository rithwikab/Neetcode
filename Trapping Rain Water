 /*using o(n) extra space for pre-processing array and o(n) time*/
        /*intuition - the sum of water stored is the result
        to get water stored at each index , we should get total water stored divided by the number of units between it
        and subtract the current height 
        but because the limiting factor is the minimum of both heights we take min of L,R
        but it's not enough to take just the two end points as those should be tall walls of a container which means they should have max height 
        so we need to find the max height from left and max height from right 
        the formula for water stored at each point is max(l,r)-h[i] */
/*
/*
class Solution {
    public int trap(int[] height) {
        int n = height.length;
        int[] left=new int[n];
        int[] right=new int[n];
        int[] min=new int[n];
        left[0]=0;right[n-1]=0;
        for(int i=1;i<n-1;i++){
            left[i]=Math.max(left[i-1],height[i-1]);
            right[n-i-1]=Math.max(right[n-i],height[n-i]);
           
        }
        for(int i=0;i<n;i++) min[i]=Math.min(left[i],right[i]);
        int sum=0;
        for(int i=0;i<n;i++){
            int val=min[i]-height[i];
            if(val>0)sum+=val;
        }
        return sum;
    }
}
*/
/* O(n) time and o(1) space */
class Solution {
    public int trap(int[] height) {
        /*take two pointers L and R and go on till L<R
        we need to calculate the min of max L and max R
        to calculate min we dont need to wait till we find 
        if current L and R are max , the min of the current 
        L and R would give the same result 
        how to move the pointer -> less traversing is key 
        if we already know the min of L,R at a point 
        we know the limiting factor so we can move the min value
        if we move the max pointer , then we might find a pointer with 
        more height -> in this case the min of L,R remains same
        less height -> it would just be submerged in the water*/
       int n=height.length;
       int leftMax=height[0];
       int rightMax=height[n-1];
       int l=0,r=n-1;
       int sum=0;
       while(l<r){
            
            if(leftMax < rightMax ){
                sum+=leftMax - height[l];
                l++;
                leftMax = Math.max(leftMax,height[l]);
            }
            else{
                sum+=rightMax-height[r];
                r--;
                rightMax=Math.max(rightMax,height[r]);
            }
       }
       return sum;

    }
}

*/
/*optimised solution using two pointers to find max left and max right */

